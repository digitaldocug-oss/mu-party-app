# Google Apps Script Web App URL that relays Registration submissions into
# the Mother's Union Google Sheet. Optional — if unset, the app behaves
# exactly as before (submissions just show the confirmation screen, nothing
# is sent anywhere). See the deploy steps shared separately for how to get
# this URL from your own Google Sheet.
NEXT_PUBLIC_SHEETS_WEBHOOK_URL=
